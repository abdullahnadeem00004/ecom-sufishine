import React from "react";

const SufiShineLogo = ({
  className = "h-10 w-auto",
}: {
  className?: string;
}) => (
  <img src="/update_logo.JPG" alt="SUFI SHINE Logo" className={className} />
);

export default SufiShineLogo;
